import React from 'react'

export const metadata = {
    title: "Aadarsh Guru - Login",
    description: "This is Login Page",
};

export default function RootLayout({ children }) {
    return (
        <>
            {children}
        </>
    )
}
